/**
 * Name of model property which holds the selected link behavior.
 */
export const LINK_TARGET_MODEL = "linkTarget";
/**
 * Name of the representation of `linkTarget` in view. Uses HTML
 * `target` attribute, so that we can easily deal with copy & paste.
 */
export const LINK_TARGET_VIEW = "target";
