{
  "name": "@coremedia/ckeditor5-logging",
  "version": "14.1.2-rc.0",
  "description": "Logging Facade",
  "keywords": [
    "coremedia",
    "ckeditor",
    "ckeditor5",
    "logging"
  ],
  "engines": {
    "node": "18",
    "pnpm": "^8.1"
  },
  "main": "./src/index.ts",
  "license": "Apache-2.0",
  "devDependencies": {
    "@coremedia-internal/ckeditor5-jest-test-helpers": "^1.0.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "clean": "pnpm clean:src && clean:dist",
    "clean:src": "npx rimraf \"src/**/*.@(js|d.ts)\"",
    "clean:dist": "npx rimraf ./dist",
    "build": "tsc --project ./tsconfig.release.json",
    "jest": "jest",
    "jest:coverage": "jest --collect-coverage",
    "npm-check-updates": "npm-check-updates --upgrade"
  }
}
