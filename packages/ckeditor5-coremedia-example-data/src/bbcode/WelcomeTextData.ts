import type { ExampleData } from "../ExampleData";

export const welcomeText = `\
[h1]CKEditor 5: CoreMedia BBCode Plugin Showcase[/h1]

This example instance of CKEditor 5 serves as a showcase for the BBCode
Plugin provided by CoreMedia (along with a so-called [i]Data Facade[/i]
plugin).

[h2]Example Data[/h2]

For testing several use-cases you will see buttons at the top, which load
different data-sets for testing and for experimenting with this CKEditor
instance and its plugins.
`;

export const welcomeTextData: ExampleData = {
  Welcome: welcomeText,
};
